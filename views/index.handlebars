<h1>Eat Da Burger!</h1>

<h2>To be Eaten.</h2>

<ul>
    {{#each burger}}
    {{#unless eaten}}
    <li>
        <p>{{name}}</p>
    </li>
    {{/unless}}
    {{/each}}
</ul>

<h2>Devoured.</h2>

<ul>
    {{#each burger}}
    {{#if eaten}}
    <p>{{name}}</p>
    {{/if}}
    {{/each}}
</ul>